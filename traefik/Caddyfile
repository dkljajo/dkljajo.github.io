{
    davidkljajo@gmail.com
    auto_https on
}

dkljajo.duckdns.org.duckdns.org {
    encode gzip

    handle_path /nextcloud/* {
        reverse_proxy nextcloud:80
    }

    handle_path /pihole/* {
        reverse_proxy pihole:80
    }

    handle_path /checkmk/* {
        reverse_proxy checkmk:5000
    }

    respond "✅ Caddy reverse proxy is running on DuckDNS!" 200
}
