{
    email davidkljajo@gmail.com
    # Optional: increase logging detail
    log {
        output stdout
        level INFO
    }
}

dkljajo.duckdns.org {
    encode gzip
    tls {
        dns duckdns {env.DUCKDNS_TOKEN}
    }

    handle_path /nextcloud/* {
        reverse_proxy nextcloud:80
    }

    handle_path /pihole/* {
        reverse_proxy pihole:80
    }

    handle_path /checkmk/* {
        reverse_proxy checkmk:5000
    }

    respond "✅ Caddy Reverse Proxy active and secured with Let's Encrypt at dkljajo.duckdns.org" 200
}
